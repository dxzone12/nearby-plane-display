[project]
name = "nearby-plane-display"
version = "0.1.0"
description = "Nicely display info about nearby planes"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
    "requests>=2.32.5",
]

[dependency-groups]
dev = []
